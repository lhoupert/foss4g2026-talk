---
description: Render the Slidev deck to per-slide images and review content + visual/layout quality
---

Use the Agent tool with `subagent_type: "slide-reviewer"` to review the presentation deck.
Pass it this prompt:

"Review the Slidev deck in the current working directory (/Users/lhoupert/DevDS/foss4g/foss4g2026-talk).
Follow your full review protocol: render slides to PNGs, inspect each one visually, cross-check content,
and return a prioritised Markdown report of concrete findings and fixes."
